{
  "name": "sunday-school-study-companion",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --pretty false",
    "deploy:azure": "bash ./scripts/deploy-azure-static-webapp.sh"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.4"
  }
}